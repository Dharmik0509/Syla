
// Config to handle API URL based on environment
// In development, this will be empty (using Vite proxy /api)
// In production, this should be the full backend URL (e.g. https://syla-backend.onrender.com)
const API_HOST = 'http://localhost:5000'; // Hardcoded for local dev

export default API_HOST;
